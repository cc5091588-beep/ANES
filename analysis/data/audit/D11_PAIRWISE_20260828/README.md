# D11 warning record

`D11_warnings.csv` is derived from the original `D11_CONDITION_LOG.csv`. It keeps all five warning records and six columns. Only the case row-number lists and the colon preceding them were removed from `message`.

Original SHA-256:

```text
620a7cbfe73b61d99676ca827951f01a13c167f7918ee4eeb49e5562c365caab
```

The original log is not included because it contains case indices. This derivative does not match its historical hash and is not substituted into the original Step 09 hash gate. The current delivery hash is in `analysis/checks/Files.csv`, relative to the repository root.
